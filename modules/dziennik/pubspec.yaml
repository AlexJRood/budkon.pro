name: dziennik
description: "Dziennik budowy — wpisy, pogoda, mapa, markety budowlane"
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../../core
  map:
    path: ../map
  flutter_riverpod: ^2.6.1
  dio: ^5.7.0
  flutter_map: ^8.1.1
  latlong2: ^0.9.1
  flutter_screenutil: ^5.9.3
  intl: any
  image_picker: ^1.2.1
