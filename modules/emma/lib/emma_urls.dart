import 'package:core/platform/url.dart';

/// emma feature API endpoints, decentralized out of core's URLs God-package.
class EmmaUrls {
  const EmmaUrls._();

static final emmaUnreadCount = URLs.appendBaseUrl('/emma/chat/unread-count/');
}
