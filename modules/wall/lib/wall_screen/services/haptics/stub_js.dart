// import 'dart:js_interop';

// @JS('navigator.vibrate')
// external void _jsVibrate(JSArray<JSNumber> pattern);

// // helper to convert Dart List<int> → JSArray<JSNumber>
// JSArray<JSNumber> _toJSPattern(List<int> pattern) =>
//     pattern.map((e) => e.toJS).toList().toJS;