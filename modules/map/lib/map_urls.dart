import 'package:core/platform/url.dart';

/// map feature API endpoints, decentralized out of core's URLs God-package.
class MapUrls {
  const MapUrls._();

static String tileCacheVersion = URLs.appendBaseUrl('/portal/tile-cache-version/');
}
