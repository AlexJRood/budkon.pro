name: map
description: "Shared map infrastructure — layers, tile caching, offline packs, map state."
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  core:
    path: ../../core
  flutter:
    sdk: flutter

  flutter_riverpod: ^2.6.1
  latlong2: ^0.9.1
  flutter_map: ^8.1.1
  flutter_map_cancellable_tile_provider: ^3.1.0
  flutter_map_tile_caching: ^10.1.1
  get: ^4.7.2
  shared_preferences: ^2.3.3
  path_provider: ^2.1.4
  http: any
  dio: any
  objectbox_flutter_libs: ^4.1.0
  path: ^1.9.1
  sqflite: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
