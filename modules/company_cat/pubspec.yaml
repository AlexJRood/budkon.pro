name: company_cat
description: "Company mascot cat — live roaming pet (faza 2)."
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  core:
    path: ../../core
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  rive: ^0.11.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/cat.riv
