import 'package:flutter/cupertino.dart';

class ClosedProjectsWidget extends StatelessWidget {
  const ClosedProjectsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
